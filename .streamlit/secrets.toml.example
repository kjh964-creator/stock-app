# Streamlit secrets 예시
# 실제로 사용하려면 이 파일을 secrets.toml 로 복사하고 값 채우기
# secrets.toml 은 절대 깃에 안 올라감 (.gitignore 등록됨)
#
# Streamlit Cloud 배포 시:
# - 대시보드 → App settings → Secrets 메뉴에서 같은 형식으로 입력

# 로그인 비밀번호 (배포 환경에서만 동작, 비워두면 로그인 패스)
password = "redsoul1310"

# Anthropic Claude API 키 (배포 시 필요 — 로컬은 .env 사용)
ANTHROPIC_API_KEY = "sk-ant-api03-..."

# (선택) Naver 검색 API
# NAVER_CLIENT_ID = "..."
# NAVER_CLIENT_SECRET = "..."

# (선택) Telegram 알림
# TELEGRAM_BOT_TOKEN = "..."
# TELEGRAM_CHAT_ID = "..."
